const products = ["silla", "mesa", "teclado"];

products.forEach(console.log);

for (const element of products) {
    console.log(element);
}

for (let i = 0; i < products.length; i++) {
    console.log(products[i]);
}